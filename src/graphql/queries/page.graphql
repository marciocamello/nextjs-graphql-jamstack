query Page($slug: String!) {
    page(
        where: {
            slug:$slug
        }
    ){
        title
        subtitle
        content {
            text
        }
        seo {
            title
            description
        }
    }
}